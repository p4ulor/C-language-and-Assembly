#include <stdio.h> //An imported library used in order to call the function printf(), preprocess directives starts with #
int main(){
	printf("Wassup world!\n"); //im tired of hello world...
	int n = 2;
	unsigned int m = 5;
	printf("%d and %u\n", n, m);
	puts("ay, you see the new line without the use of \\n?");
	return 0;
}