Format of the ':' for parameters:

Example: vf::o:

v -> optional, no parameter (no : after letter)

f -> optional, can contain parameter (has : after letter)

o -> mandatory, can contain parameter (has : BEFORE and after letter)

